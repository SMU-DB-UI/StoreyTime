export class Group {
    constructor (creator_id, group_name, member_count) {
        this.creator_id = creator_id;
        this.group_name = group_name;
        this.member_count = member_count;
    }
}

export default Group;